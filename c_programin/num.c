#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: This function demonstratements and printing.
 *              of 'num' is less than or equal to 10 and
 *              `prints a message accordingly.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;

	for (x = 1; x <= 5; x++)
	{
		printf("%d\n", x);
	}
		return (0);

}
